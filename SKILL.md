---
name: job-fit-compass
description: 岗位匹配度分析 + 求职机会匹配 + 简历优化。输入岗位职责和用户现状（专业、课程、技能、证书、项目经历），生成五页Tab式HTML报告——Tab1职责×知识点映射、Tab2岗位知识全景、Tab3提升建议（差距优先热力图 + P0/P1/P2 + 带真实学习资源的学习计划 + 学习顺序 + 3个月路线图）、Tab4求职机会（基于简历反向验证真实招聘链接，按匹配度/公司规模/招聘截止时间排序，公司风险聚焦成立时间/社保人数/劳动仲裁/其他风险并尽力核查、统一匹配度口径、附薪资市场参考、候选清单落盘可续跑）、Tab5简历优化（基于JD用STAR法则重写经历、对齐总览、缺口桥接、输出可复制优化版简历）。借鉴 ai-job-search 的 /upskill 思路做两遍差距分析与优先级排序。JD+背景模式直接出报告；简历模式为交互式两段：先定位职位区间供用户选择，选定后再拉取该岗位JD做深度匹配与简历优化。当用户提供JD和自身背景对比能力差距、或提供简历希望搜索匹配的公司与岗位、或希望基于目标JD优化简历时使用。
agent_created: true
---

# 岗位匹配度分析器

## 简介 / Description

**中文**：面向求职者的岗位匹配分析技能。输入简历后先定位职位区间供选择，再抓真实 JD 出五页 HTML 报告（职责×知识点匹配 / 知识全景 / 差距热力图＋真实学习资源 / 求职机会含反向验证与风险核查 / STAR 简历优化）；风险聚焦成立时间、社保人数、劳动仲裁及其他风险，缺失标"待核"，全程不编造。
**English**: A job-fit analysis skill for job seekers. It locates a range of positions for you to pick, then fetches the real JD and produces a 5-tab HTML report (duty×knowledge mapping / knowledge landscape / gap heatmap + real resources / job opportunities with reverse-verified links + risk check / STAR resume optimization). Risk checks cover founding date, social-security headcount, labor arbitration and other risks; missing data is flagged "待核", with no fabrication.

## 触发条件

用户提供以下信息时触发：
- 岗位职责（JD 文字或截图）
- 自身背景（专业、课程、技能、证书、经历）

**求职机会匹配（交互式两段流程）** 在以下情况触发：用户提供了**简历/背景**并希望"找合适的公司与岗位"。这是一段式触发、两段式执行：
- **第一段（定位职位区间）**：用简历推断目标方向 → 搜索并反向验证公开岗位 → 按匹配度/规模/截止排序 + 风险核查 → 把候选职位区间呈现给用户，**请用户选择**想深入的岗位（此阶段不生成 Tab1-3、不输出完整报告）。
- **第二段（获取 JD 并出报告）**：用户选定后，拉取该岗位 JD（优先取已验证链接原文，取不到则多源交叉验证，仍不足请用户粘贴）→ 用真实 JD 重算精确匹配并跑第一步～第五步生成 Tab1-3 → 输出完整 4-Tab 报告。

## 输入校验

若用户只提供了 JD 但没有自身背景，**必须先追问**，不要直接生成结果：

> 请告诉我你的情况：专业、主要课程、掌握的技能（语言/工具/框架）、证书、项目经历——这样我才能做针对性的匹配分析。

若用户只提供了模糊背景（如"我是计算机专业"），继续追问具体课程和技能点，确保有足够的粒度做知识点级映射。

**简历文件原生解析（.docx）**：若用户提供的是 `.docx` 简历，**优先用 python-docx 做结构化解析**（保留段落、表格、标题层级），不要用手工 unzip + ElementTree 直接读 XML——后者遇表格/页眉/分节会丢内容或错位。可用 `pip install python-docx` 安装后编写提取脚本；若环境受限（无 python-docx 或无法安装），退而求其次提示用户直接粘贴简历文本。

若用户希望**搜索匹配的公司与岗位**（提供简历而非 JD），先确认三件事：**目标岗位方向、意向城市、行业偏好**。若缺失则追问；若用户只说"帮我找合适的公司"，先根据简历推断 1-3 个目标岗位方向列出让用户确认。确认后执行第一段（搜索 + 反向验证 + 排序 + 风险核查），把候选职位区间呈现给用户并**请其选择**想深入的岗位——此阶段**不生成完整报告**，只交付候选清单供挑选。

## 分析流程

### 第一步：解析岗位职责

将每条职责拆成 1-3 个具体工作模块，每个模块用人话写一句"实际做什么"。

示例：
- JD原文"借助数据分析手段制定增长策略并落地" → 拆为"数据层面的商家画像""建模层面的商家分级""资源分配建模""KPI预测"四个模块

### 第二步：知识点级映射（角色无关定义）

对每个模块，列出所需的**具体知识点**，然后对照用户背景判断掌握程度。

> **知识点定义（角色无关，I）**：知识点 = 岗位职责落到的最小可执行单元——可以是一段**操作/话术**、一个**系统模块名**、一项**流程节点**、一个**证书名**、或一行**代码/函数**。它**不限于代码**，非技术岗同样成立：
> - 数据分析岗：`pandas.groupby().agg()`、`KMeans`、`scipy.optimize.linprog`
> - 服务顾问/客服岗：`来店接待七步法`（话术）、`DMS 工单录入`（系统模块）、`异议处理话术`、`客户投诉升级流程`、`汽车构造基础`（证书/知识）
> - 技工岗：`万用表测量`、`PLC 接线`、`设备点检清单`
> 落点原则：**能被"当场演示或口述"的具体动作**，而非"沟通能力""数据分析"这类抽象词。

每行判断结果用以下四种状态标记：
- **绿色 p-g**：用户课程/技能中已有
- **红色 p-r**：用户未学过，岗位需要，需补
- **蓝色 p-b**：非必需但加分
- **粉色 p-p**：软技能，学校教育不覆盖

> **能力向量（一次抽取，全程复用，策略1）**：进入分析前，先从简历/背景**一次性**抽出一个结构化"能力向量"——技能/工具/证书清单 + 每项熟练度（已精通 / 接触过 / 未接触）+ 教育层次 + 关键经历。该向量在 Tab1-3 复用，也对第一段每个候选岗位做廉价匹配打分（类似 ai-job-search `/rank`），**无需对每岗重复分析**。

### 第三步：生成练手项目

每个模块配一个具体的、可放到 GitHub 的练手项目。要求：
- 能独立完成（不需要企业环境）
- 有明确的输入、输出和技术栈
- 适合写在简历上

### 第四步：知识全景

整理这个岗位所需的**全部知识点**（不限用户已学），分两块：

**硬技能**按领域分类：数据获取、数据处理、分析与建模、可视化、数据工程、工具链。每项标绿色/红色/蓝色。

**软技能**列出 5-6 项，每项必须包含"学生现在就能练"的具体场景：
- 场景必须来自：课程大作业、数学建模比赛、学生会/社团、毕业论文、课堂展示、刷JD/看行业报告
- 不能写"在工作中积累"，必须是学生身份能做的事

### 第五步：差距热力图与提升建议（借鉴 ai-job-search 的 /upskill 思路）

先做一次**两遍差距分析**，再产出优先级热力图、带真实资源的学习计划与学习顺序。

**两遍差距分析**
- **Pass1 · 硬技能 diff**：直接汇总第二步映射表中标记为 `p-r`（需补）的每个知识点，形成硬技能缺口清单。匹配时**从宽**处理——若用户背景以任何形式提到该技能（如"Python"涵盖"Python 脚本"），不计入缺口，避免误报。
- **Pass2 · LLM 综合**：补全 Pass1 会漏掉的隐性缺口，每条打标签 `[domain]` / `[soft]` / `[tooling]` / `[credential]`：
  - `[domain]` 行业/领域知识（如网络安全、量化金融、气候科技）
  - `[soft]` 工作方式与沟通（如跨团队对齐、向上汇报）
  - `[tooling]` 工具链/流程（如 MLOps、CI/CD、大规模敏捷）
  - `[credential]` 岗位偏好的证书（如 AWS SA、PMP）
  - 不要与 Pass1 重复。

**差距优先热力图（heatmap）**
将 Pass1 + Pass2 合并为一张优先级表，列：优先级 / 技能·领域 / 类型 / 来源。定级：
- **Critical**：高频硬技能缺口，或跨多数岗位出现的领域缺口
- **High**：中频硬技能，或持续出现的软/工具缺口
- **Medium**：低频硬技能，或仅少数岗位出现的综合缺口
- **Low**：一次性提及的 nice-to-have
- **待核（pr-u）**：某项数据/信号缺失、无法定级时，仅列已知事实，不强行排优先级

类型列用小型标签：硬/Hard、领域/domain、软/soft、工具/tooling、证书/credential。此表先作为中间产物展示给用户，再继续生成下方学习计划。

**学习资源（WebSearch）**
对 **Critical 与 High**（缺口少于 5 个时含 Medium）每项缺口，用 **WebSearch 查找 2-3 个真实、当前高质量资源**：
- 查询带**当前年份**（如 `2026 学 Kubernetes 课程 推荐 site:coursera.org OR site:reddit.com`），避免陈旧结果
- 优先：带动手实验的课程 > 官方文档 > 书籍
- 为每个资源写「名称 + 链接 + 一句话适配理由」
- **绝不编造**：只引用 WebSearch 真实返回的结果，无结果则省略，不要虚构课程/URL/作者
- 写一句**定制学习路径**：基于用户已有背景说明该跳过的模块与起点（如"你已会 Docker，直接看调度与网络章节"）
- 给出**达到可用水平的预估耗时**（如 ~20h），宁可估多
- 若用户要求离线或跳过资源检索，则省略链接，仅保留方向与耗时

**提升卡片 P0/P1/P2**
将热力图缺口映射进三档（P0=Critical/High 面试必补；P1=Medium 加分；P2=Low/工作中积累），每张卡片含：学什么、怎么练（学生场景）、达标标准、周期、**资源链接（若有）**。

**学习顺序（study order）**
在卡片之后加一张「学习顺序」表（# / 主题 / 类型 / 预计耗时 / 备注），规则：
1. **依赖前置**：学 B 需先会 A（如"AWS 网络"依赖"AWS 基础"），A 在前并注明依赖
2. **优先级**：同层内 Critical > High > Medium
3. **速赢优先**：约 ~5h 的 Medium 可提前，提升信心
4. **领域/软技能靠后**：结合真实项目练，不单独前置
末尾给出**总耗时**。

**3 个月路线图**
底部保留三列路线图：第 1 月打基础、第 2 月出作品、第 3 月投递。

### 第七步：简历优化（基于 JD + STAR 法则）

当分析已拿到真实 JD（第二步映射完成）且用户提供了**可结构化拆解的经历**（简历文件解析结果，或对话中明确给出的具体实习/项目）时执行，产出 Tab「简历优化」。若用户只给了技能清单而无经历，STAR 重写受限，仅做"缺口桥接 + 简历结构建议"并提示补充经历。分四块：

**① 对齐总览（RESUME_ALIGN）**
把 JD 关键要求逐条列出，对照简历证据标注状态：
- **强证据（align-strong，绿）**：经历直接覆盖，可改写为 STAR 亮点
- **弱证据（align-weak，琥珀）**：沾边但缺细节/量化，需补强后才成亮点
- **缺口（align-gap，珊瑚红）**：无对应经历 → 转 Tab3（P0/P1）补能力；简历侧用"相关课程/学习中"诚实占位
每行：JD 要求 | 简历证据 | 状态 | STAR 可用（✓/△/—）。

**② STAR 重写（STAR_REWRITE）**
对每条"强/弱证据"经历，做 STAR 改造并给出可直接粘贴的简历条目：
- **S 情境**：一句话背景（部门/规模/业务场景/痛点）
- **T 任务**：你被要求达成的具体目标
- **A 行动**：你实际做了什么——落到第二步的知识点（`DMS 工单录入`、`异议处理话术`），动词开头
- **R 结果**：量化产出（提升 X% / 节省 Y 小时 / 0 差错处理 Z 单）。**无数据也必须写"⚠ 待补量化"，绝不编造数字**
展示结构：原始表述（灰条）→ STAR 四要素（S/T/A/R 带字母徽章）→ 优化后简历条目（绿条，一行式）。
弱证据额外给"补强建议"（如"补充你用 X3 系统处理过多少条录入、准确率多少"）。

**③ 缺口桥接（GAP_BRIDGE）**
对"缺口"项，结合 Tab3 的 P0/P1：
- 1-2 个月可补 → 建议一个可写进简历的短期实践（"2 周完成 XX 课 + 小项目，写'在学/已认证'"）
- 需长期 → 简历诚实标"相关课程/学习中"，不伪造成经历
- 每条桥接回 Tab3 对应卡片编号（如 P0-1 / P1-2）

**④ 优化后简历全文（OPT_RESUME）**
Tab5 末尾给一份**干净、可复制**的优化版简历正文：沿原简历结构 + STAR 重写条目 + 缺口诚实占位，放进 `.opt-resume` 块，方便用户一键复制。
约束：STAR 的 R 不编造数字；缺口项不伪造成经历；优化版保留原简历真实信息，只重排与补全话术。

### 第六步：求职机会匹配（简历模式 · 交互式两段流程）

仅在用户给出简历并希望"找合适的公司与岗位"时执行。一段式触发、两段式执行：先帮用户定位"职位区间"并让其选择，选定后再拉取该岗位 JD 做深度匹配与简历优化。这与"JD+背景"模式不同——后者可直接跑第一步～第七步（含简历优化）。

> **能力向量（一次抽取，全程复用）**：进入第一步前，先**一次性**从简历抽出一个结构化"能力向量"——技能/工具/证书清单 + 每项熟练度（已精通 / 接触过 / 未接触）+ 教育层次 + 关键经历。该向量在 Tab1-3 复用，也对第一段每个候选岗位做廉价匹配打分（类似 ai-job-search `/rank`），无需对每岗重复分析。

**第一段：定位职位区间（呈现候选，等待选择）**

先用第一步～第二步的方法把简历 + 能力向量 转成 **目标岗位画像**（需要的知识点清单），据此搜索公开招聘信息：

**① 搜索、聚合与初筛（WebSearch）**
- 按"目标岗位 + 意向城市 + 行业"检索，覆盖：企业官网招聘页 / 官方校招公告、BOSS直聘、智联招聘、猎聘、拉勾等**持牌招聘平台**。
- 每条结果记录：公司名、岗位名、链接、来源平台、公司规模（人数/营收档）、招聘截止时间（如有）。
- **同公司跨平台聚合（去重 G）**：同一公司多条岗位先按公司聚合，保留**最优链接**（官方 > BOSS > 智联 > 其它持牌），并在来源处并列标注出现过的平台（如 `官网 · BOSS`）。不把同一公司拆成多行。

**② 反向验证 + 新鲜度校验（强制，不做则不得列入清单）**
- 每条链接**必须**来自官方/持牌渠道，域名为该平台真实域名（BOSS→`zhipin.com`、智联→`zhaopin.com`、官网→公司自有域名）。
- 查询时主动带平台名偏置到官方页（如 `"公司名 招聘 site:zhipin.com"`）。
- **新鲜度校验（J）**：列出前对每条链接做一次可达性检查——用 `curl -I -s -o /dev/null -w "%{http_code}" <url>`（或 WebFetch 试探）。返回 404 / 403 墙后无内容 / 页面显示"已关闭""已招满""该职位不存在" → **判为失效**，直接移入"已筛除"并写原因，不进入候选清单。
- 验证方式：核对域名与来源一致、页面可打开、岗位真实存在。**无法核实或来自论坛/自媒体/未核实聚合站/转帖的链接一律不采用**，标 `待核` 放入"已筛除"或省略，绝不以猜测 URL 充数。
- 每行带 `.verify` 来源徽章：`v-site`=官网/官方校招、`v-boss`=BOSS直聘、`v-zl`=智联招聘、`v-other`=其它持牌平台。

**③ 综合排序（匹配度 / 公司规模 / 招聘截止时间）**
- **方向契合度（第一段）**：用能力向量 ÷ 推断目标岗位画像估算百分比，作为主排序键。明确这是"你与该方向的整体契合"，非某条具体 JD 命中率。
- **公司规模**：按人数分档（1万+ / 1000-1万 / 100-1000 / <100）作为次键。
- **招聘截止时间**：越临近越优先（升序）作为第三键。
- 三者加权：匹配度降序 → 规模降序 → 截止升序，给出综合排名 `#`，1 为最优先。

**④ 公司风险核查与筛除（想尽办法获取，实在无数据才标待核）**
对进入清单的每家公司，用 WebSearch 多渠道**尽力**查公开记录，重点核查四项并填"公司风险核查"表：
- **成立时间**：国家企业信用信息公示系统 / 企查查 / 天眼查
- **参保人数（社保）**：企查查 / 天眼查"参保人数"
- **劳动仲裁**：中国裁判文书网 / 企查查 / 天眼查"劳动争议"开庭公告与裁判文书数量（注意区分原告/被告身份，常规纠纷不轻易定高）
- **其他风险**：经营异常名录、失信被执行人（老赖）、行政处罚、严重违法失信名单、股权冻结/清算等
- **获取策略**：先查聚合工商平台（企查查/天眼查/水滴信用），再交叉到国家企业信用公示系统、裁判文书网核实；同公司不同名称/简称都要试。**查不到某项 → 该行标 `待核`，不臆造、不硬判中/低**（B）。
- 风险定级（**提示而非判决 H**，语言中性化）：
  - **高风险（risk-high）**：成立 < 1 年且参保 = 0；或近 2 年劳动争议 ≥ 5 起且多为被告；或列入经营异常/失信被执行人 → **提示不推荐，移入"已筛除"**
  - **中风险（risk-mid）**：成立 1-3 年且参保 < 20；或 1-4 起劳动争议；或参保与公开规模明显不符；或出现经营异常但已移出 → **提示关注点**
  - **低风险（risk-low）**：成立 > 3 年、参保稳定、无显著争议、无其他风险 → **可正常投递**
  - **待核（risk-u）**：成立/参保/仲裁/其他 任一项查不到 → 不分级，仅列已知事实 + "公开数据缺失，请自行核实"
- **已筛除表**：透明列出被筛掉的公司/岗位及具体原因（成立时间、参保、仲裁、其他风险等公开信号），不静默丢弃。
- **免责声明**：风险数据来自公开渠道，可能滞后或不全，仅作辅助判断，投递前请用户自行核实；只列公开事实，不做主观定性。

**⑤ 候选清单落盘（F，会话可中断续跑）**
第一段呈现前，把候选清单写入工作目录文件 `{简历名}_候选岗位.md`（含公司/岗位/链接/来源/方向契合度/规模/截止/风险等级/已验证）。用户在第二段只需说"选 #N"，即可读取该文件续跑，无需重新搜索。

第一段产出后：**把候选"职位区间"呈现给用户**——可用「求职」Tab 表格，或对话列出并请用户选择（如"回复公司+岗位名或编号"）。**此阶段不生成 Tab1-3、不输出完整 HTML**。

**第二段：获取选定岗位 JD 并生成完整报告**

用户选定（回复编号或公司名）后：

**⑥ 获取该岗位 JD（降级链 + 可信度标注 C / 策略2）**
按以下降级链获取，**绝不猜 URL**：
1. **WebFetch 第一段已验证链接**拿岗位原文；
2. 若链接在登录墙/反爬（如 BOSS 常拦截）→ **WebSearch**（"`岗位名` `公司名` 岗位职责 要求"）补全；
3. **多源交叉验证**：同时比对 51job / 智联 / qcc / 公司官网 等同源 JD，优先采用**带真实可打开链接**且多源一致的文本；
4. 仍不足 → **请用户粘贴 JD 文本**（给明确提示）。
- **JD 来源可信度**标注（写进报告 Tab1）：`jd-high`（原链接可读到原文）/ `jd-mid`（多源一致摘要，至少 1 个带真实链接）/ `jd-low`（仅单一摘要、无链接可验证）。
- 拿到 JD 后用真实 JD **重算精确匹配度**（覆盖第一段"方向契合度"，写回求职清单对应行，A / 策略7）。

**⑦ 生成 Tab1-3 + Tab5**：用真实 JD + 能力向量 + 简历，执行第一步～第五步（匹配映射 / 知识全景 / 差距热力图 + 资源 + 顺序 + 路线），并执行**第七步简历优化**（STAR 重写 + 对齐总览 + 缺口桥接 + 优化版全文）填充 Tab5。Tab1 末尾附**薪资参考（E）**：若 JD 含薪资，再用 WebSearch 抓同岗位同城市的 3-5 条公开薪资，给出市场区间与分位（如"6-10K，市场中位数约 8K"），无数据则省略。
**⑧ 输出完整 5-Tab HTML**：Tab4 保留第一段职位区间（选定行标 `◆已选` 且匹配度已更新为精确值）、风险核查、已筛除；Tab1-3 由选定 JD 填充（含薪资参考与 JD 来源可信度）；Tab5 由第七步填充。文件名 `{岗位名称}能力匹配分析.html`，调用 present_files 展示。

## HTML 生成

### 模板

使用 `assets/template.html` 作为基础模板。模板包含完整的 CSS 和 JS（五页 Tab 切换），内容区用 `<!-- SECTION: xxx -->` 和 `<!-- END: xxx -->` 标记需要替换的段落。生成时读取模板，替换标记之间的内容。

模板标记说明：
- `<!-- SECTION: JOB_TITLE -->` 到 `<!-- END: JOB_TITLE -->`：替换为岗位名称
- `<!-- SECTION: JOB_TITLE2 -->` 到 `<!-- END: JOB_TITLE2 -->`：替换为岗位名称（标题区）
- `<!-- SECTION: TABLE_ROWS -->` 到 `<!-- END: TABLE_ROWS -->`：替换为映射表行（通常 8-12 行）
- `<!-- SECTION: HARD_SKILLS -->` 到 `<!-- END: HARD_SKILLS -->`：替换为硬技能卡片（6 张）
- `<!-- SECTION: SOFT_SKILLS -->` 到 `<!-- END: SOFT_SKILLS -->`：替换为软技能卡片（5-6 张）
- `<!-- SECTION: SALARY -->` 到 `<!-- END: SALARY -->`：替换为薪资参考框（市场区间 + 分位；JD 无薪资时整块留空省略）
- `<!-- SECTION: JD_META -->` 到 `<!-- END: JD_META -->`：替换为 JD 来源可信度一行（jd-high/jd-mid/jd-low 徽章 + 获取方式）；JD+背景模式可省略
- `<!-- SECTION: HEATMAP_ROWS -->` 到 `<!-- END: HEATMAP_ROWS -->`：替换为差距优先热力图表（4-8 行，优先级 pill 用 pr-c/pr-h/pr-m/pr-l/pr-u，类型用 .tp 标签）
- `<!-- SECTION: P0_SKILLS -->` 到 `<!-- END: P0_SKILLS -->`：替换为 P0 提升卡片（可含 .sk-res 资源列表）
- `<!-- SECTION: P1_SKILLS -->` 到 `<!-- END: P1_SKILLS -->`：替换为 P1 提升卡片（可含 .sk-res 资源列表）
- `<!-- SECTION: P2_SKILLS -->` 到 `<!-- END: P2_SKILLS -->`：替换为 P2 提升卡片（可含 .sk-res 资源列表）
- `<!-- SECTION: STUDY_ORDER -->` 到 `<!-- END: STUDY_ORDER -->`：替换为学习顺序表（5-8 行，末行为总耗时）
- `<!-- SECTION: ROADMAP -->` 到 `<!-- END: ROADMAP -->`：替换为 3 个月路线图
- `<!-- SECTION: JOBLIST_ROWS -->` 到 `<!-- END: JOBLIST_ROWS -->`：替换为匹配岗位清单（6-12 行，每行含综合排名 # / 公司名 + `.verify` 来源徽章 + 真实链接 / 岗位名 / `.match` 匹配度条 / 公司规模 / `.dl` 截止时间 pill / `.risk` 风险 pill；选定行加 `◆已选` 标记且匹配度为精确值）
- `<!-- SECTION: RISK_ROWS -->` 到 `<!-- END: RISK_ROWS -->`：替换为公司风险核查表（每行：公司 / 成立时间 / 参保人数 / 劳动仲裁 / 其他风险 / `.risk` 风险等级[含 risk-u 待核] / 结论建议）
- `<!-- SECTION: FILTERED_ROWS -->` 到 `<!-- END: FILTERED_ROWS -->`：替换为已筛除表（每行：公司·岗位 / 筛除原因，含失效链接与高风险信号）
- `<!-- SECTION: RESUME_ALIGN -->` 到 `<!-- END: RESUME_ALIGN -->`：替换为对齐总览表（8-12 行，状态 pill 用 align-strong/align-weak/align-gap，末列 STAR 可用 ✓/△/—）
- `<!-- SECTION: STAR_REWRITE -->` 到 `<!-- END: STAR_REWRITE -->`：替换为 STAR 重写卡片（2-4 张，每张含原始表述灰条 + S/T/A/R 四要素带字母徽章 + 优化后简历绿条；弱证据附补强建议）
- `<!-- SECTION: GAP_BRIDGE -->` 到 `<!-- END: GAP_BRIDGE -->`：替换为缺口桥接卡片（2-4 张，每张含缺口项 + 桥接建议 + 回链 Tab3 卡片编号 P0-x/P1-x）
- `<!-- SECTION: OPT_RESUME -->` 到 `<!-- END: OPT_RESUME -->`：替换为优化后简历全文（可复制 `.opt-resume` 块，沿原简历结构 + STAR 条目 + 缺口诚实占位）

资源链接写法（在 P0/P1/P2 卡片的 `.sk-body` 内追加）：
```
<div class="sk-res"><span class="rs-l">学习资源</span>
<a href="https://..." target="_blank">资源名称</a> · 一句话理由<br>
<a href="https://..." target="_blank">资源名称</a> · 一句话理由</div>
```

JD 来源可信度徽章写法（在 JD_META 区块内）：
```
<span class="jd-badge jd-high">JD可信度·高</span> 取自原招聘链接原文
```

薪资参考框写法（在 SALARY 区块内）：
```
<div class="salary-box">该岗位 JD 薪资 <span class="sal-num">6-10K</span>；同城市同岗位公开薪资区间约 <b>5-12K</b>，中位数约 <b>8K</b>（样本 4 条，来源 BOSS/智联）。</div>
```

### 设计约束

生成时必须遵守以下规则（模板 CSS 已内置）：

1. **极简主义**：页面标题只有岗位名+"能力分析"小标签。无副标题、无图例、无 note 提示框、无 icon 装饰。留白代替说明文字。
2. **颜色即信息**：pill 的颜色本身就是答案，不配图例。p-g=已有（绿）、p-r=需补（红/珊瑚）、p-b=加分（蓝）、p-p=非课程（粉）。
3. **Tab 命名**：五个 Tab 按钮的文案固定为"匹配""知识""提升""求职""简历"五个词，不加 tag 标签。
4. **代码标签**：所有技术名词使用 `class="kp"` 的等宽字体标签；非技术岗同理用 kp 标"话术/系统模块/流程节点"等最小可执行单元。
5. **表格**：每行知识点列的末尾必须有一个颜色 pill 标明用户掌握程度，和知识全景页的颜色体系统一。
6. **软技能卡片**：每张必须含一个 `.sc-scene` 场景框，标题为"现在就能练"，内容是学生可接触的具体场景。
7. **提升建议**：P0/P1/P2 用颜色区分（lv0 珊瑚红/lv1 琥珀/lv2 蓝），3 个月路线图用三列卡片布局；P0/P1/P2 卡片内可含 `.sk-res` 真实资源链接（来自 WebSearch，不编造）。
8. **差距热力图（heatmap）**：优先级用颜色 pill（pr-c 珊瑚红=Critical / pr-h 琥珀=High / pr-m 蓝=Medium / pr-l 灰=Low / pr-u 灰=待核），类型用小型 `.tp` 标签；表格无图例，颜色即信息，与 P0/P1/P2 体系统一（Critical↔lv0、High↔lv1、Medium↔lv2）。
9. **学习顺序表（study order）**：斑马纹表格，列 #/主题/类型/预计耗时/备注，末行为「总耗时」加粗行；与 3 个月路线图并存（微观顺序 + 宏观时间）。
10. **求职清单（Tab4）**：岗位清单用 `.job-table`，每行必须含综合排名 `#`、`.verify` 来源徽章（v-site/v-boss/v-zl/v-other，标注链接真实来源）、真实可打开链接、`.match` 匹配度条（内联 `style="width:NN%"`）、公司规模、`.dl` 截止时间 pill（dl-urgent/dl-soon/dl-ok）、`.risk` 风险 pill（risk-low/risk-mid/risk-high/risk-u）。排序严格按 匹配度降序 → 规模降序 → 截止时间升序。**同公司已按公司聚合去重，不拆多行（G）**。
11. **反向验证 + 新鲜度（强制）**：清单中**只放**企业官网/官方校招公告或 BOSS直聘/智联招聘等持牌平台的真实链接，域名须与 `.verify` 徽章一致；论坛/自媒体/未核实聚合站链接一律不采用，改放"已筛除"。列出前须做可达性校验，失效链接（404/已关闭/已招满）直接进"已筛除"（J）。
12. **公司风险核查（Tab4）**：`RISK_ROWS` 必须覆盖 **成立时间 / 参保人数 / 劳动仲裁 / 其他风险** 四项公开数据并给 `.risk` 等级（含 `risk-u` 待核）；高风险公司移入 `FILTERED_ROWS` 并写明原因，不静默丢弃。语言为**中性提示**（可投递 / 需关注 / 提示不推荐），不做判决式表述。底部保留一句免责声明（数据来自公开渠道、可能滞后、投递前请自行核实）。
13. **求职 Tab 的说明文字**：仅在两处允许极简说明——清单下方一行"链接均经反向验证、排序规则"；风险表下方一行"数据来源与免责"。其余保持无图例、颜色即信息。
14. **简历模式两段式纪律**：第一段只交付候选职位区间并请用户选择，**不得**在用户选定前生成 Tab1-3 或输出完整 HTML；第二段拿到真实 JD 后才跑第一步～第五步并出完整报告。Tab4 在第一段即可呈现，Tab1-3 留待第二段填充；选定行的匹配度须在第二段重算为精确值（A/策略7）。
15. **薪资参考（E）**：仅当 JD 含薪资时，在 Tab1 末尾 `SALARY` 区块给出同城市同岗位公开薪资区间与分位；无数据则省略整块，不编造区间。
16. **候选清单落盘（F）**：第一段须把候选写进 `{简历名}_候选岗位.md`，第二段可读取续跑，会话中断不丢状态。
17. **JD 来源可信度（C/策略2）**：Tab1 的 `JD_META` 区块须标注 `jd-high/jd-mid/jd-low` 可信度与获取方式，让用户知道 JD 文本的可信程度。
18. **知识点去技术化（I）**：非技术岗的知识点须落到话术/系统模块/流程节点/证书等最小可执行单元，不得强行用代码函数示例。
19. **简历优化 Tab（Tab5）**：对齐总览表用 `.align-table`，状态 pill 用 align-strong(绿)/align-weak(琥珀)/align-gap(珊瑚红)；STAR 重写用 `.star-card`（原始表述灰条 `.resume-before` + S/T/A/R 字母徽章 `.sb-s/.sb-t/.sb-a/.sb-r` + 优化后绿条 `.resume-after`）；缺口桥接 `.gap-card` 须回链 Tab3 卡片编号；末尾 `.opt-resume` 给可复制全文。
20. **STAR 真实性**：R（结果）不得编造数字，缺量化必须标"⚠ 待补量化"提示；缺口项不得伪造成经历，简历中只能诚实标"相关课程/学习中"。
21. **简历优化触发条件**：两种模式均产出 Tab5——JD+背景模式需用户提供了经历（实习/项目），纯技能清单无经历则仅给结构建议并提示补经历；简历模式第二段天然具备经历，直接跑第七步。
22. **输出 Tab 数**：完整报告为 5 个 Tab（匹配/知识/提升/求职/简历），`sw()` 按索引自动切换，新增 Tab 无需改 JS。

### 输出

- **JD+背景模式**：完成第一步～第七步后，保存 `{岗位名称}能力匹配分析.html`（5 个 Tab）并调用 present_files 展示。
- **简历模式**：第一段只呈现候选"职位区间"（对话或临时表格），并写入 `{简历名}_候选岗位.md`，**不写完整报告文件**；第二段用户选定并拿到真实 JD 后，才生成完整 5-Tab HTML（Tab4 保留职位区间与风险核查、选定行标 `◆已选` 且匹配度为精确值，Tab1-3 由选定 JD 填充、含薪资参考与 JD 来源可信度，Tab5 由第七步填充）并 present_files。
